#include "handrank.h"
   int main()
   {
     HandRank hr(2);
     hr.CreateHandMap();
     // for(int numPlayers=2;numPlayers<11;numPlayers++){
     // HandRank hr(numPlayers);
     // printf("Number of Players: %d\n",numPlayers);
     // ///     hr.PrintHand();
     // //     hr.CheckDistrib();
     // //     hr.PrintGame();
     // hr.BuildStat();
     // }
     return 0;
   }
